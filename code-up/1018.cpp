#include <iostream>
using namespace std;

int main() {
    int a, b;
    // scanf("%d:%d", &a, &b);
    // printf("%d:%d", a, b);
    char tmp;
    cin >> a >> tmp >> b;
    cout << a << tmp << b;

    return 0;
}